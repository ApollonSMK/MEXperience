import { MetadataRoute } from 'next';

export default function robots(): MetadataRoute.Robots {
  return {
    rules: {
      userAgent: '*',
      allow: '/',
      disallow: ['/admin/', '/profile/', '/api/'], // Proteger áreas privadas
    },
    sitemap: 'https://me-experience.lu/sitemap.xml',
  };
}