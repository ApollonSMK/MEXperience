
import { Sun, Waves, Dna, Sunrise, Wrench } from 'lucide-react';
import type { LucideIcon } from 'lucide-react';

export const iconMap: { [key: string]: LucideIcon } = {
  Sun,
  Waves,
  Dna,
  Sunrise,
  default: Wrench,
};
